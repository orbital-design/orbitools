{
  "$schema": "https://orbitools/schemas/module.v1.json",
  "slug": "orbital-login",
  "name": "Orbital Login",
  "description": "Restyle the wp-login.php screen with the Orbital brand — navy backdrop, white card, blue primary button, Orbital logo in place of the WordPress mark.",
  "version": "1.0.0",
  "category": "modules",
  "class": "\\Orbitools\\Modules\\Orbital_Login\\Orbital_Login",
  "default_enabled": false,
  "requires": {
    "php": ">=7.4",
    "wp": ">=5.0"
  },
  "settings": [
    {
      "id": "logo_url",
      "type": "text",
      "label": "Logo link URL",
      "description": "Where the login logo links to. Leave blank to keep the WordPress default (wordpress.org).",
      "default": "https://orbital.co.uk",
      "placeholder": "https://orbital.co.uk"
    },
    {
      "id": "logo_title",
      "type": "text",
      "label": "Logo title attribute",
      "description": "Title text on the login logo link. Leave blank to keep the WordPress default (\"Powered by WordPress\").",
      "default": "Orbital",
      "placeholder": "Orbital"
    },
    {
      "id": "remember_last_user",
      "type": "toggle",
      "label": "Remember the last user on this device",
      "description": "After a successful login, store the username in a per-device cookie (30 days). On the next visit to wp-login.php, greet the user by name, hide the username field, and ask only for the password. Tradeoff: anyone with access to the browser learns a valid username. A 'Not you?' link clears the cookie. Password is always required — this is purely a UX shortcut.",
      "default": false
    }
  ]
}
