/**
 * Video block save function.
 *
 * Server-rendered — render_callback in Video.php produces the
 * markup. Save returns null so nothing's stored in the post HTML
 * beyond the block delimiter + attributes.
 */
export default function Save(): null {
    return null;
}
