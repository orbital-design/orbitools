/**
 * Reading Time — save function.
 *
 * Server-rendered (render_callback in Reading_Time.php). Save
 * returns null so nothing's stored in post HTML beyond the block
 * delimiter + attributes.
 */
export default function Save(): null {
    return null;
}
